# Brand Kit Generator - Environment Variables
# Copy this file to .env.local and fill in your actual keys

# --- Gemini (Google AI) ---
# Get your API key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: override default models
GEMINI_TEXT_MODEL=gemini-2.5-flash
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image-preview

# --- ElevenLabs (TTS + Music) ---
# Get your API key: https://elevenlabs.io/
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# --- fal.ai (Veo-3-Fast video) ---
# Get your API key: https://fal.ai/
FAL_AI_KEY=your_fal_ai_key_here

# --- Rate limiting (Upstash Redis) ---
# Disable limit guards entirely (for demos): set to true
RATE_LIMIT_DISABLED=false
# Max runs per IP per day (only used if Redis is configured and not disabled)
RATE_LIMIT_MAX_PER_IP_PER_DAY=2
# Upstash Redis REST credentials (optional; if not set, guard is bypassed)
UPSTASH_REDIS_REST_URL=https://your-upstash-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# --- Cloudflare R2 (asset uploads) ---
# Account ID is visible in Cloudflare dashboard
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
# Create an R2 API token with write access to your bucket
R2_UPLOAD_IMAGE_ACCESS_KEY_ID=your_r2_access_key_id
R2_UPLOAD_IMAGE_SECRET_ACCESS_KEY=your_r2_secret_access_key
# The bucket where assets are uploaded
R2_UPLOAD_IMAGE_BUCKET_NAME=your_bucket_name
# Public access base URL (when bucket is public). Example: https://pub-xxxxxxxxxxxxxxxxxxxxxxxxxxxx.r2.dev
R2_PUBLIC_BASE_URL=

# --- Server ---
# API server port for local dev (matches server/index.mjs default)
PORT=8787
# Node environment (development/production)
NODE_ENV=development

# --- Database (Neon Postgres) ---
# Optional, enables persistent share storage with Drizzle ORM
DATABASE_URL=postgresql://user:password@hostname/dbname
